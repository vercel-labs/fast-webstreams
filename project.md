# Fast WebStream for Node.js streams

WebStreams in node are pretty slow.

- Could we make them faster?
- Any features we'd need to drop?
- Can we implemnt purely in Rust and use napi? Or would that actually be slower?